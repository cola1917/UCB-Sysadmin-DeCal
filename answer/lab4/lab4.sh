# part zero
# done

# part one
# 1.rsync 文件同步 hwclock.sh 读取硬件时钟 ssh 计算机之间通信、远程登录
# 2.reload 与 restart reload 热重启，部分请求仍然由旧服务处理，当检查完配置文件后，由新服务处理请求，当配置有问题时会仍然由就旧服务处理请求，restart 硬重启。
# 3.done 遇到了没权限的问题 sudo service nginx start
# 4.没能run起来，codespace中没有systemctl
# 5.done

# part two
# yes use cpu to much
# done